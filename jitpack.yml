jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.12-open
  - sdk use java 17.0.12-open

install:
  - ./gradlew clean :payload_reader:publishToMavenLocal :payload_writer:publishToMavenLocal :library:publishToMavenLocal :plugin:publishToMavenLocal -x test
