jdk:
  - openjdk17

install:
  - export ANDROID_HOME=/opt/android-sdk-linux && export ANDROID_SDK_ROOT=/opt/android-sdk-linux && ./gradlew clean :payload_reader:publishToMavenLocal :payload_writer:publishToMavenLocal :library:publishToMavenLocal :plugin:publishToMavenLocal -x test